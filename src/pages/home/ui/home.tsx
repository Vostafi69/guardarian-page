import { Button } from "@/shared/ui/button";
import { Logo } from "@/shared/ui/logo";
import { FC } from "react";

export const Home: FC = () => {
  return (
    <>
      <Logo />
      <Button size="small">Integrate Us</Button>
    </>
  );
};
